version: '3.8'
services:
  payload-generator:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - ./output:/app/output
    environment:
      - DOTNET_ENVIRONMENT=Development
    command: ["--type", "Command", "--validate"]
