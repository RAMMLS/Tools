# PowerShell Payload Generator - Образовательный инструмент для специалистов по безопасности

## ⚠️ ВНИМАНИЕ: ПРЕДУПРЕЖДЕНИЕ О БЕЗОПАСНОСТИ И ЛЕГАЛЬНОСТИ

**ЭТОТ ИНСТРУМЕНТ ПРЕДНАЗНАЧЕН ТОЛЬКО ДЛЯ:**
- ✅ Образовательных и исследовательских целей
- ✅ Тестирования собственных систем с явного письменного разрешения
- ✅ Отработки навыков в контролируемых лабораторных условиях

**СТРОГО ЗАПРЕЩЕНО ИСПОЛЬЗОВАТЬ ДЛЯ:**
- ❌ Тестирования систем без явного разрешения владельца
- ❌ Любых незаконных или вредоносных действий
- ❌ Обхода систем безопасности без авторизации

**Использование этого инструмента для тестирования систем без явного разрешения является незаконным и может повлечь уголовную ответственность.**

## 📋 Содержание
- [Обзор проекта](#-обзор-проекта)
- [Архитектура](#-архитектура)
- [Быстрый старт](#-быстрый-старт)
- [Примеры использования](#-примеры-использования)
- [Docker Compose](#-docker-compose)
- [Разработка и тестирование](#-разработка-и-тестирование)
- [Типы полезных нагрузок](#-типы-полезных-нагрузок)
- [Безопасность и аудит](#-безопасность-и-аудит)
- [Лицензия и контакты](#-лицензия-и-контакты)

## 🎯 Обзор проекта

PowerShell Payload Generator - это инструмент на C#/.NET 6 для безопасной генерации PowerShell скриптов, предназначенных для легального тестирования безопасности, аудита и автоматизации административных задач.

**Ключевые возможности:**
- Генерация различных типов PowerShell скриптов
- Автоматическое кодирование и обфускация
- Проверка синтаксиса и безопасности
- Система аудита и логирования
- Контейнеризация через Docker

## 🏗️ Архитектура

```
PowerShellPayloadGenerator/
├── src/PowerShellPayloadGenerator/
│   ├── Models/           # Модели данных
│   │   ├── PayloadConfig.cs
│   │   ├── PayloadType.cs
│   │   ├── GenerationResult.cs
│   │   ├── AuditRecord.cs
│   │   └── SecurityContext.cs
│   ├── Services/         # Бизнес-логика
│   │   └── PayloadService.cs
│   ├── Utilities/        # Вспомогательные функции
│   │   └── SecurityHelper.cs
│   ├── Program.cs        # Точка входа
│   └── PowerShellPayloadGenerator.csproj
├── docker/               # Docker конфигурации
├── scripts/              # Вспомогательные скрипты
├── output/               # Сгенерированные скрипты
└── tests/                # Тестовые сценарии
```

## 🚀 Быстрый старт

### Требования
- Docker 20.10+ или .NET 6.0 SDK
- PowerShell 7.0+ (для тестирования скриптов)
- 500 МБ свободного места

### Способ 1: Запуск через Docker (рекомендуется)

```bash
# 1. Соберите Docker образ
docker build -t payload-generator .

# 2. Проверьте, что образ создан
docker images | grep payload-generator

# 3. Запустите базовые тесты
docker run --rm payload-generator --type Discovery
docker run --rm payload-generator --type SecurityAudit
docker run --rm payload-generator --type Command --command "Get-Service"
```

### Способ 2: Локальный запуск

```bash
# 1. Перейдите в директорию проекта
cd src/PowerShellPayloadGenerator

# 2. Восстановите зависимости
dotnet restore

# 3. Соберите проект
dotnet build -c Release

# 4. Запустите генератор
dotnet run -- --type Discovery
```

## 📊 Примеры использования

### Пример 1: Обнаружение информации о системе
```bash
docker run --rm payload-generator --type Discovery
```
**Вывод (пример):**
```powershell
=== System Information Discovery ===
Write-Host '=== System Information ===' -ForegroundColor Green
systeminfo | Select-String 'OS Name','OS Version','System Type','Total Physical Memory'
...
```

### Пример 2: Проверка безопасности
```bash
docker run --rm payload-generator --type SecurityAudit --validate
```
**Вывод включает:**
- Проверку политик учетных записей
- Статус брандмауэра
- Конфигурацию служб
- Открытые сетевые ресурсы

### Пример 3: Сканирование портов
```bash
docker run --rm payload-generator --type PortScan --ip 127.0.0.1
```

### Пример 4: Кодированная полезная нагрузка
```bash
docker run --rm payload-generator --type Discovery --encode
```
**Результат:** PowerShell скрипт в формате Base64, который можно выполнить так:
```powershell
powershell -EncodedCommand "BASE64_STRING_HERE"
```

## 🐳 Docker Compose

Для запуска полноценной тестовой среды с несколькими сервисами:

### Исправленный `docker-compose.yml`:
```yaml
services:
  generator:
    image: payload-generator:latest
    build: .
    container_name: payload-generator
    volumes:
      - ./output:/app/output
    environment:
      - DOTNET_ENVIRONMENT=Production
    command: ["--type", "SecurityAudit", "--validate"]
    networks:
      - payload-network
    restart: unless-stopped

  tester:
    image: mcr.microsoft.com/powershell:latest
    depends_on:
      - generator
    volumes:
      - ./tests:/tests
    networks:
      - payload-network
    command: ["pwsh", "-c", "Write-Host 'PowerShell test environment is ready'"]
    restart: "no"

networks:
  payload-network:
    driver: bridge
```

### Команды управления:
```bash
# Запуск всей системы
docker compose up -d

# Просмотр логов генератора
docker compose logs -f generator

# Остановка системы
docker compose down
```

## 🔧 Разработка и тестирование

### Сборка образа для разработки
```bash
docker build -t payload-generator-dev -f Dockerfile.dev .
```

### Запуск с hot-reload (автоматическая пересборка при изменениях)
```bash
docker run -it --rm \
  -v ${PWD}:/app \
  -v ${PWD}/output:/app/output \
  payload-generator-dev --type Discovery
```

### Makefile для автоматизации
```bash
# Доступные команды
make build        # Собрать основной образ
make run-discovery # Запустить Discovery
make dev-run      # Запустить в режиме разработки
make compose-up   # Запустить docker-compose стэк
make compose-logs # Показать логи
make compose-down # Остановить стэк
```

### Тестирование всех типов нагрузок
```bash
# Запустите тестовый скрипт
pwsh scripts/test-all.ps1
```

## 🛡️ Типы полезных нагрузок

| Тип | Уровень риска | Описание | Пример использования |
|-----|---------------|----------|---------------------|
| **Discovery** | Info | Обнаружение информации о системе | `--type Discovery` |
| **SecurityAudit** | Info | Проверка настроек безопасности | `--type SecurityAudit` |
| **Command** | Low | Выполнение произвольной команды | `--type Command --command "Get-Process"` |
| **PrivilegeEnumeration** | Low | Перечисление привилегий и групп | `--type PrivilegeEnumeration` |
| **VulnerabilityCheck** | Info | Базовая проверка уязвимостей | `--type VulnerabilityCheck` |
| **PortScan** | Medium | Сканирование портов (только с разрешения!) | `--type PortScan --ip 192.168.1.1` |
| **LogCollection** | Info | Сбор и экспорт логов | `--type LogCollection` |
| **ReverseShell** | High | Обратная оболочка (ТОЛЬКО для тестовых лабораторий!) | `--type ReverseShell --ip 192.168.1.100 --port 4444` |

## 🔐 Безопасность и аудит

### Встроенные механизмы безопасности

1. **Проверка разрешений**:
   ```csharp
   // Автоматическая проверка разрешенных типов нагрузок
   if (!_securityContext.IsPayloadTypeAllowed(config.Type))
   {
       throw new SecurityException("Тип нагрузки запрещен");
   }
   ```

2. **Аудит всех операций**:
   - Все генерации логируются с метаданными
   - Хэши SHA256 всех сгенерированных скриптов
   - Временные метки и информация о пользователе

3. **Валидация синтаксиса**:
   ```bash
   # Автоматическая проверка синтаксиса
   docker run --rm payload-generator --type Discovery --validate
   ```

### Политика использования

1. **Требуется явное разрешение** на тестирование любой системы
2. **Использование только в изолированных средах** для тестирования
3. **Обязательное документирование** целей и результатов тестирования
4. **Соблюдение законодательства** вашей страны

## 📝 Лицензия и контакты

### Лицензия
Этот проект распространяется под лицензией **Educational Use Only**. Использование в коммерческих целях запрещено без специального разрешения.

### Ответственное использование
Если вы обнаружили уязвимость с помощью этого инструмента:
1. Не эксплуатируйте ее без разрешения
2. Сообщите владельцу системы через ответственный канал раскрытия
3. Предоставьте подробный отчет с рекомендациями

### Контакты для ответственного раскрытия
- **Email**: security@example.com
- **PGP Key**: доступен по запросу
- **Ответственный срок**: 48 часов на подтверждение получения

### Авторы и вклад
- **Разработчик**: Senior Security Engineer
- **Цель проекта**: Образовательная демонстрация принципов безопасности
- **Требования к вкладу**: Все запросы на добавление функциональности должны включать описание законного использования

---

## 🎓 Образовательные ресурсы

### Рекомендуемые курсы
- [Offensive Security Certified Professional (OSCP)](https://www.offensive-security.com/pwk-oscp/)
- [SANS SEC560: Network Penetration Testing](https://www.sans.org/cyber-security-courses/network-penetration-testing/)
- [eLearnSecurity Junior Penetration Tester (eJPT)](https://elearnsecurity.com/product/ejpt-certification/)

### Лаборатории для практики
- [Hack The Box](https://www.hackthebox.com/)
- [TryHackMe](https://tryhackme.com/)
- [PentesterLab](https://pentesterlab.com/)

### Документация
- [Microsoft PowerShell Documentation](https://docs.microsoft.com/en-us/powershell/)
- [MITRE ATT&CK Framework](https://attack.mitre.org/)
- [OWASP Testing Guide](https://owasp.org/www-project-web-security-testing-guide/)

---

**ПОМНИТЕ:** Знания в области безопасности несут ответственность. Используйте свои навыки для защиты систем, а не для их компрометации.

*Последнее обновление: $(date +%Y-%m-%d)*
